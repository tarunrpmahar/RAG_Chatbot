# RAG Chatbot 🤖

This is a Retrieval-Augmented Generation (RAG) based chatbot built using Python.

## Features
- Context-aware responses
- Uses vector search + LLM
- Modular and extensible design

## Setup
```bash
pip install -r requirements.txt